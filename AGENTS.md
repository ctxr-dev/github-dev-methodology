# AGENTS.md

This repository is **github-dev-methodology** — a portable, agent-agnostic methodology that uses GitHub Issues + Projects as the canonical source of truth for AI-assisted engineering work.

If you're an AI agent reading this repo:

- **Start here:** [`index.md`](index.md). It lists every topic in recommended read order.
- **Working in a consumer project that installed this methodology:** the same `index.md` is your entry point. The install prompt in the [README](README.md) sets up the `.agents/ctxr-dev/github-dev-methodology/` layout so this file is reachable from the consumer's `AGENTS.md` at their project root.

See [README.md](README.md) for the human-facing overview.
